# Vercel 手动配置指南 - 必读！

## ⚠️ 重要说明

**Vercel 仍然在使用 pnpm**，即使代码中已经删除了 pnpm 配置。必须在 Vercel Dashboard 中手动配置使用 npm。

---

## 🔧 解决步骤（必须在 Vercel Dashboard 中操作）

### 第一步：访问 Vercel 项目设置

1. 登录 [vercel.com](https://vercel.com)
2. 进入 `happy-house` 项目

### 第二步：手动设置构建命令

1. 点击顶部菜单的 **Settings**
2. 左侧选择 **General**
3. 找到 **Build & Development Settings**

**重要！必须手动设置以下内容：**

| 字段 | 设置值 |
|------|--------|
| Framework Preset | Next.js |
| Install Command | `npm install` |
| Build Command | `npm run build` |
| Output Directory | `.next` |

⚠️ **不要**让这些字段为空或使用自动检测！**必须**手动填写！

### 第三步：保存并清理缓存

1. 点击 **Save** 按钮
2. 进入 **Deployments** 标签
3. 找到最近的部署，点击 **...** → **Redeploy**
4. 勾选 **"Skip cache"**
5. 点击 **Redeploy**

---

## ❓ 为什么需要手动配置？

Vercel 的自动检测可能会：
- 从缓存中读取旧的包管理器设置
- 检测到远程仓库中残留的 pnpm-lock.yaml
- 优先使用之前配置过的包管理器

**手动设置是唯一可靠的方式！**

---

## ✅ 验证配置是否生效

重新部署后，查看构建日志：

**✅ 成功（使用 npm）：**
```
> npm install
added xxx packages in xx s
> npm run build
```

**❌ 失败（使用 pnpm）：**
```
> pnpm install
ERR_PNPM_META_FETCH_FAIL
```

---

## 🔍 如果仍然失败

### 检查以下几点

1. **确认保存成功**
   - 返回 General 设置页面
   - 确认 Install Command 显示为 `npm install`
   - 确认 Build Command 显示为 `npm run build`

2. **删除 Vercel 缓存**
   - Deployments → 选择失败的部署 → Redeploy → Skip cache
   - 或者在 Settings → Functions 中清除缓存

3. **检查 Environment Variables**
   - 删除所有 `PNPM_*` 相关的环境变量
   - 添加 `NPM_FLAGS` = `--legacy-peer-deps`（可选）

4. **删除 vercel.json 中的 pnpm 配置**
   - 确保 vercel.json 没有 `installCommand`
   - 确保 vercel.json 没有 pnpm 相关字段

---

## 💻 本地开发不受影响

本地开发仍然可以使用 pnpm：

```bash
pnpm install
pnpm dev
```

Vercel 部署使用 npm，两者互不冲突。

---

## 📞 需要帮助？

如果按照上述步骤操作后仍然失败：

1. 查看完整的构建日志
2. 记录错误信息
3. 检查 Vercel Dashboard 中的配置是否真的保存了
4. 尝试删除项目重新导入

---

## 🎯 总结

**必须在 Vercel Dashboard 中手动设置：**
- Install Command: `npm install`
- Build Command: `npm run build`

**不要依赖自动检测！**
